<div class="drawer">
  <input id="my-drawer" type="checkbox" class="drawer-toggle" />
  <div class="drawer-content">
    <!-- Page content here -->
    <header class="header-container">
      <nav class="absolute py-4 px-4">
        <div class="max-w-6xl mx-auto flex justify-start items-center">
          <label for="my-drawer" class="btn btn-square btn-ghost drawer-button rounded-2xl">
            <svg class="swap-off fill-current" xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 512 512">
              <path d="M64,384H448V341.33H64Zm0-106.67H448V234.67H64ZM64,128v42.67H448V128Z"/>
            </svg>
          </label>
        </div>
      </nav>
    </header>
  </div>
  <div class="drawer-side z-50">
    <label for="my-drawer" aria-label="close sidebar" class="drawer-overlay"></label>
    <aside class="bg-[#8b4513] min-h-full w-80 p-4">
      <ul class="mt-10 menu text-white text-4xl">
        <li><a href="/" class="font-medium text-white hover:text-orange-200">HOME</a></li>
        <li><a href="https://www.variety.gifts/shop" class="font-medium text-white hover:text-orange-200">SHOP</a></li>
        <li><a href="/contact" class="font-medium text-white hover:text-orange-200">CONTACT</a></li>
      </ul>
    </aside>
  </div>
</div>

<style>
  /* Header Container */
  .header-container {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
  }

  /* Navigation */
  .navbar {
    background: transparent;
    box-shadow: none;
  }

  /* Hamburger button styling */
  .drawer-button {
    background-color: var(--fallback-bc,oklch(var(--bc)/0.2));
  }

  .drawer-button svg {
    color: white;
  }

  .drawer-button:hover svg {
    opacity: 0.8;
  }

  /* Ensure menu items are clickable */
  .drawer-side aside {
    z-index: 60;
  }

  .drawer-side .menu li a {
    position: relative;
    z-index: 70;
  }
</style>

